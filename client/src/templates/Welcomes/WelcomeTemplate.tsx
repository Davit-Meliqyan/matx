const WelcomeTemplate = () => {
  return (
    <div className="h-full w-full p-7 flex items-center justify-center flex-col">
      <h1 className="text-3xl font-bold text-center">Welcome to MatX</h1>
      <span className="text-lg font-medium text-center">Your all-in-one solution for smart business management.</span>
      <span className="text-lg font-medium text-center">
        Customize your workspace, assign roles, and manage data — exactly the
        way your company needs.
      </span>
    </div>
  );
};

export default WelcomeTemplate;
